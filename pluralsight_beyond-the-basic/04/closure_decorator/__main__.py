from pprint import pprint as pp
from sort_by_last_letter import sort_by_last_letter

sorted_list = sort_by_last_letter(['hello', 'from', 'a', 'local', 'function'])
sorted_list = sort_by_last_letter(['hello', 'from', 'a', 'local', 'function'])
sorted_list = sort_by_last_letter(['hello', 'from', 'a', 'local', 'function'])
pp(sorted_list)

